[build]
builder = "nixpacks"

[deploy]
startCommand = "npm install && npm start"

[environments.production]
builder = "nixpacks"

[volumes]
# Optional: Add volume for data persistence if needed
# data = "/app/data"
